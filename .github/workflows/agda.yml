name: Build
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ⬇️
        uses: actions/checkout@v3
      - name: Build 🛠️ 
        container: totbwf/agda-mugen-ci:latest
