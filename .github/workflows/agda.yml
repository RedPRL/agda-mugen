name: Build
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  run:
    runs-on: ubuntu-latest
    container:
      image: totbwf/agda-mugen-ci:latest
    steps:
      - name: Checkout ⬇️
        uses: actions/checkout@v3
      - name: Build 🛠️ 
        run: make build
